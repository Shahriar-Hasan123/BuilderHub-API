# Work Log — 2026-07-30

## Summary
- Added `ImageOptimizer` service and supporting DRF mixin for upload-time image compression.
- Added nested site image CRUD endpoints under `/api/v1/sites/{site_pk}/images/`.
- Updated changelog to document the new site image API and route fix.

## Tasks
- Implemented `apps/core/services/image_optimizer.py` for image compression and format handling.
- Added `ImageOptimizationMixin` in `apps/core/mixins.py` to compress validated image fields in DRF serializers.
- Added `SiteImageSerializer` and `SiteImageListAPIView` / `SiteImageDetailAPIView`.
- Added nested `site_pk` image routing in `apps/sites/urls.py` and fixed the list route parameter.
- Updated `CHANGELOG.md` with site images API documentation.

## Notes
- `ImageOptimizer` currently skips SVG and GIF, compresses large PNGs, and converts opaque PNGs to JPEG.
- `ImageOptimizationMixin` validates compressed payload size after optimization and wraps failures in serializer validation errors.
- Route parameter mismatch (`pk` on list endpoint) was corrected to `site_pk`.

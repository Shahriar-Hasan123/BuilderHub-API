# Work Log — 2026-08-07

## Summary
- Extended publish rollback reliability by restoring site appearance metadata and making rollback fully transactional.

## Tasks
- Updated `HTMLToJSONConverter.convert_header()` so header snapshots now include `favicon`, `logo`, `thumbnail`, and `global_css` storage names.
- Updated rollback restore to use header JSON metadata for site appearance and to preserve only the pages present in the selected version.
- Changed rollback behavior so pages deleted after the target version are resurrected and pages added after it are disabled, instead of aborting rollback.
- Added transactional rollback semantics: restore + republish now run inside a single database transaction to avoid partial state when failures occur.
- Hardened publish version allocation by locking the `Site` row with `select_for_update()` during version creation.
- Verified `publish()` asset write paths are consistent with the published asset snapshot and orphan cleanup logic.

# Work Log — 2026-07-29

## Summary
- Updated publish service behavior for safer republishing and atomic status updates.
- Recorded changelog entries for publish improvements and bug fixes.

## Tasks
- Added `CHANGELOG.md` entries under `Fixed` for publish service changes.
- Created `docs/worklog/2026-07-29.md` with today's work log.

## Notes
- Publish now uses temp-then-swap writes to avoid partial failure asset loss.
- Orphaned page JSON files are cleaned up after publish.
- Database updates are wrapped in a transaction only after successful file writes.
- Empty header/footer files now fail validation with a 400 response.

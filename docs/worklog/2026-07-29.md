# Work Log — 2026-07-29

## Summary
- Updated publish service behavior for safer republishing and atomic status updates.
- Recorded changelog entries for publish improvements and bug fixes.

## Tasks
- Added `CHANGELOG.md` entries under `Fixed` for publish service changes.
- Created `docs/worklog/2026-07-29.md` with today's work log.

## Notes
- Publish now uses temp-then-swap writes to avoid partial failure asset loss.
- Orphaned page JSON files are cleaned up after publish.
- Database updates are wrapped in a transaction only after successful file writes.
- Empty header/footer files now fail validation with a 400 response.
- HTMLMinifier now collapses whitespace at the DOM text-node level and preserves script/style/pre/textarea content.
- Added HTMLMinifier-specific unit tests for malformed tags, preservation blocks, and inline-element spacing.
- Added site publish tests for unauthenticated 401, empty header/footer content rejection, disabled-page exclusion, orphaned page JSON cleanup on republish, failed-republish preserving live assets, stronger minify assertions, and exact JSON schema validation for response and asset files.
- Added comprehensive image validation and media fields: `ImageValidator`, `Site.thumbnail`, `Page.hero_image`, and stronger favicon/logo validators.

# Work Log — 2026-07-31

## Summary
- Refactored `ImageOptimizationMixin` into a standalone `ImageFieldProcessor`
  service, decoupled from serializer inheritance.

## Tasks
- Added `apps/core/utils/image_field_processor.py` with `ImageFieldProcessor.process()`.
- Removed `ImageOptimizationMixin` from `apps/core/mixins.py` (kept `SiteLockMixin` as-is).
- Updated `SiteSerializer` and `SiteImageSerializer` (`apps/sites/serializers.py`)
  and `PageSerializer` (`apps/pages/serializers.py`) to call `ImageFieldProcessor`
  directly from their own `create()`/`update()` methods.
- Centralized Swagger/OpenAPI schema definitions in `apps/core/schema.py` and switched the auth, site, and page views to use the shared schema helpers.
- Standardized API endpoints to use a no-trailing-slash convention by updating routing and documentation examples.
- Updated `CHANGELOG.md` to document the refactor.

## Notes
- Pure refactor — compression logic, size-check thresholds, and validation
  error messages are unchanged; only the class location and calling convention
  moved from mixin-inheritance to explicit composition.
